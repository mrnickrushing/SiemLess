# Development / testing dependencies (not installed in production Docker image)
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0          # async test client for FastAPI
pytest-cov>=5.0.0      # coverage reporting
